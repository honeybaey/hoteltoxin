mixin subscription-text-field(text, name)

  .subscription-text-field__wrapper
    label(for='subscribe' class='subscription-text-field__title') 
      p=name
    input(id='subscribe' type="email" name='some-info' class='subscription-text-field__input' placeholder= text)