extends ../../pug/index

include ../../blocks/header/header
include ../../blocks/footer/footer
include ../../blocks/login/login

block variables
  - const title = 'Login';

block content
  +header({
      isLoggedIn: false,
      menuItems: [
          {name: 'О нас', href: '#'},
          {name: 'Услуги', href: '#'},
          {name: 'Вакансии', href: '#'},
          {name: 'Новости', href: '#'},
          {name: 'Соглашения', href: '#'}
      ],
    })
  main.login-main
    .login-main__container.container
      section.login-card
        +login('Войти', 'login card')
  +footer()